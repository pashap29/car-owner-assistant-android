Goal:
Implement settings, profile, and future-feature placeholders.

Context:
Read settings, feature flags, and product docs.

Constraints:
- Include editable profile fields.
- Include placeholders for premium, cloud sync, family access, auth methods.
- Include app settings for startup mode, language, mileage units, fuel units, backup settings.
- Do not implement real cloud/auth logic.

Done when:
- settings screens exist
- profile fields are editable locally
- placeholders are visible and structured
- app settings persist
- navigation to settings subsections works

Output format:
1. files changed
2. implemented screens
3. verification steps
4. assumptions